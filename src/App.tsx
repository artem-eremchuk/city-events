import React from 'react';
import { Button } from '@mui/material';

function App() {
  return (
    <div>City Events
      <Button
        variant="contained"
        sx={{border:"2px solid white"}}>
          Click
      </Button>
    </div>
  );
}

export default App;
